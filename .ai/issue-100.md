# Доработка решения WordCount Pro из предыдущего PR.

### Контекст

В предыдущем PR было реализовано решение задачи wordcount (подсчёт слов, строк и символов).
Текущее решение корректно работает и используется как база.

### Задача

Необходимо ДОРАБОТАТЬ существующую реализацию, а не переписывать её с нуля.

### Требуемые изменения

1. Расширить текущую логику:
   - добавить поддержку подсчёта символов с учётом пробелов и переводов строк
   - поведение должно соответствовать утилите `wc`

2. Интерфейс функций и файловую структуру:
   - сохранять без изменений, если это возможно
   - новые функции добавлять только при необходимости

3. Тесты:
   - дополнить существующие тесты
   - не удалять уже имеющиеся тесты
   - новые тесты должны покрывать добавленную функциональность

### Ограничения

- ❌ Не переписывать реализацию с нуля
- ❌ Не менять публичные имена функций без необходимости
- ❌ Не удалять существующие файлы
- ✅ Минимальные, локальные изменения

### Критерий готовности (Definition of Done)

- Все существующие тесты проходят
- Новые тесты покрывают новую функциональность
- CI не падает
- Изменения минимальны и легко читаемы

### Примечание

Это **итеративная доработка**, а не новая задача.

Generated: 2026-01-30 18:12:26.061501