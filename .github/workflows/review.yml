name: AI Review

on:
  workflow_run:
    workflows: ["PR CI"]
    types: [completed]

jobs:
  review:
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'pull_request' &&
      github.event.workflow_run.pull_requests[0].number != null

    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ github.token }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install .

      - name: Run reviewer
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          LLM_CONFIG_JSON: ${{ secrets.LLM_CONFIG_JSON }}
          CI_WORKFLOW_CONCLUSION: ${{ github.event.workflow_run.conclusion }}
          CI_WORKFLOW_NAME: ${{ github.event.workflow_run.name }}
        run: |
          python -m app.main review-apply --pr "${{ github.event.workflow_run.pull_requests[0].number }}"